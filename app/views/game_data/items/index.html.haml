= link_to("新規#{@items.first.class.model_name.human}作成", new_game_data_item_path)
%br
- @items.each do |item|
  [#{item.kind}]#{item.name}
  = link_to("編集", edit_game_data_item_path(item.id))
  %br
= paginate @items
