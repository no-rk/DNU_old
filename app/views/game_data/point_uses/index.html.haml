= link_to("新規#{@point_uses.first.class.model_name.human}作成", new_game_data_point_use_path)
%br
- @point_uses.each do |point_use|
  [#{@point_uses.first.class.model_name.human}]
  #{point_use.name}
  = link_to("編集", edit_game_data_point_use_path(point_use.id))
  %br
= paginate @point_uses
