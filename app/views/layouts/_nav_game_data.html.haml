.sidebar-nav
  %ul.nav.nav-list
    %li{:class => active_current(game_data_index_path)}
      %a{:href => game_data_index_path} ゲームデータトップ
    %li.nav-header 最初に決めるべきもの
    - [:statuses, :diseases].each do |table|
      %li{:class => active_current(send("game_data_#{table}_path"))}
        %a{:href => send("game_data_#{table}_path")}= "GameData::#{table.to_s.classify}".constantize.model_name.human
    %li.nav-header 後で決めても良いもの
    - [:maps, :sups, :traps, :skills, :abilities, :items, :characters, :enemy_lists, :enemy_territories, :events].each do |table|
      %li{:class => active_current(send("game_data_#{table}_path"))}
        %a{:href => send("game_data_#{table}_path")}= "GameData::#{table.to_s.classify}".constantize.model_name.human
