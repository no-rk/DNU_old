.hero-unit
  %h1 Dragon NUnavut
  %p 定期更新ゲームを作る過程の何か
  - unless user_signed_in?
    %p
      %a.btn.btn-primary.btn-large{:href => new_user_registration_path} 新規登録 &raquo;
- if Day.last.present?
  %p
    第#{Day.last.day}回
    - case Day.last.state
    - when 0
      %span.badge.badge-important 更新中
    - when 1
      %span.badge.badge-warning 未確定
    - when 2
      %span.badge.badge-success 確定済
  %p #{Day.last.updated_at.to_s(:jp)}更新
