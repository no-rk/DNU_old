<%= form_for(@register_main) do |f| %>
  <% if @register_main.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@register_main.errors.count, "error") %> prohibited this register_main from being saved:</h2>

      <ul>
      <% @register_main.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
