<%= form_for(@register_main) do |form_main| %>
  <%= render :partial => 'register/error', :locals=>{:register_error=>@register_main} %>

  <% unless @read_only %>
  <div class="actions">
    <button data-remote="true" class="btn btn-large btn-warning">確認する</button>
    <%= form_main.submit :class => "btn btn-large btn-success" %>
  </div>
  <% end %>
<% end %>
