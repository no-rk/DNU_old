%br
= form_main.label :disposes
= form_main.simple_fields_for :disposes, :wrapper => :simple, :defaults => { :as => (:plain if @read_only), :user => current_user, :input_html => { :class => :'span2' } } do |form_dispose|
  = form_dispose.input :number
