%br
= form_main.label :shouts
= form_main.simple_fields_for :shouts, :defaults => { :as => (:plain if @read_only), :user => current_user, :input_html => { :class => :'span6' } } do |form_shout|
  = form_shout.input :volume, :input_html => { :class => :span2 }
  = form_shout.input :message, :input_html => { :class => :'document span9', :rows => 2 }
  %br
