= form_main.simple_fields_for :diary, :defaults => { :as => (:plain if @read_only), :user => current_user, :input_html => { :class => :'span6' } } do |form_diary|
  %br
  = form_diary.input :diary, :input_html => { :class => :'document span9', :rows => 5 }
