%h1= @register_trades.first.class.model_name.human.downcase
- @register_trades.each do |register_trade|
  .alert
    = render 'form', { :register_trade => register_trade }
    = link_to 'Show', register_trade
= paginate @register_trades
